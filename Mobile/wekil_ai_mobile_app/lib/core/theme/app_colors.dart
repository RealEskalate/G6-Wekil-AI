// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Light
  static const Color primary = Color(0xFF0D2A4B);      // Deep Blue
  static const Color background = Color(0xFFF7F9FB);   // Light gray/white
  static const Color accent = Color(0xFF2EC4B6);       // Teal
  static const Color textDark = Colors.black87;
  static const Color textLight = Colors.white;

  // Dark
  static const Color primaryDark = Color(0xFF1B3A57);
  static const Color backgroundDark = Color(0xFF121212);
  static const Color accentDark = Color(0xFF2EC4B6); // can keep same
  static const Color textDarkMode = Colors.white70;
}
